import React, {PropTypes} from 'react';

export default class Perfil extends React.Component {
  render() {
    return (
      <div className="container texto-gris">
        Perfil
      </div>
    );
  }
}

Perfil.propTypes = {
};
